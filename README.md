In this project I used mockAPI which has given json format data to interact live with reduxtoolkit CRUD. in this manner mockapi endpoint will become my database 
also added search funtionality alongside
